<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <ImageView
        android:id="@+id/applicationicon"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="TODO"
        android:src="@drawable/ic_checked" />

    <TextView
        android:id="@+id/appldatetime"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/applicationicon"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Ημερομηνία - Ώρα αίτησης"
        android:textSize="10sp"
        android:textStyle="italic" />

    <TextView
        android:id="@+id/applicationTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/appldatetime"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/applicationicon"
        android:gravity="center_vertical"
        android:text="Τίτλος Αίτησης"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="2"
        android:lines="2" />

    

</RelativeLayout> 